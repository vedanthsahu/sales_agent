## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Configure backend environment in `sales-assist-backend/.env` (LLM keys live only on the backend).
3. Run the app:
   `npm run dev`
